One-line: use wherever a real photograph will go, until one is supplied — the 135° hatch, never a grey rectangle.

```jsx
<Placeholder height={160} caption="Nanzen-ji aqueduct" />
```

Radius follows the media token (14px) by default; pass `radius="card"` inside a 6px card.
