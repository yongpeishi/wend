The Wend lock-up — use wherever the brand signs a screen, document or export.

```jsx
<Logo size={40} />
<Logo variant="reversed" size={28} showWordmark={false} />
```

Clear space on all sides equals the diameter of the largest dot. Never straighten the path, never add a fourth stop, never fill the apricot ring. Minimum mark height 24px.
