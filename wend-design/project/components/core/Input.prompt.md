Text entry. Placeholders ask a plain question rather than naming the field: "Where are you going?" not "Destination".

```jsx
<Input placeholder="Where are you going?" hint="↵" />
<Input value="Kyoto" focused />
```
