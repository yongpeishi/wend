#!/usr/bin/env bash
# Static type check, per ADR-2 (doc/architecture.md). Sorbet + Tapioca.
# See doc/assumptions.md A2 for why (and for the RBS + Steep fallback path
# this project did NOT need to take).
set -euo pipefail
cd "$(dirname "$0")/.."

bundle exec srb tc
