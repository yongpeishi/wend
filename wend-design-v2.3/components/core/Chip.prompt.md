Interest and category filters. Selection is additive — chips widen the result set rather than narrowing it. Chips are always tappable; a read-only count such as "Saved · 12" is a `<Label tone="saved">`, not a chip.

```jsx
<Chip selected>Temples</Chip>
<Chip>Long walks</Chip>
```
